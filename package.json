{
  "name": "read-a-loud",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "firebase": "^5.8.1"
  },
  "scripts": {
    "start": "cd client && yarn start",
    "predeploy": "cd client && yarn run build"
  }
}
